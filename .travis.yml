language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
cache: pip
before_install:
  - pip install pytest-cov
install:
  - pip install codecov
  - pip install -r requirements.txt
script:
  - coverage run wplay/wplay.py
  - travis_retry pytest --cov=.
after_success:
  - codecov
